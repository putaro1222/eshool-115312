<%= render "shared/header" %>

<%# 講義の概要 %>
<div class="item-show">
  <div class="item-box">
    <h2 class="name">
      <%= @curriculum.class_name%>
    </h2>
    <div class="item-img-content">
      <%= image_tag @curriculum.image ,class:"item-box-img" %>
    </div>

    
    <% if user_signed_in? %>
      <% if current_user.id == @curriculum.user_id %>
        <%= link_to "講義の編集", edit_curriculum_path(@curriculum.id), method: :get, class: "item-red-btn" %>
        <%= link_to "削除", curriculum_path(@curriculum.id), method: :delete, class:"item-destroy" %>
      <% else current_user.id != @curriculum.user_id %>
        <%= link_to "受講画面に進む", curriculum_studies_path(@curriculum.id) ,class:"item-red-btn"%>
      <% end %>
    <% end %>
    

    <table class="detail-table">
      <tbody>
        <tr>
          <th class="detail-item">投稿者</th>
          <td class="detail-value"><%= @curriculum.user.nickname %></td>
        </tr>
        <tr>
          <th class="detail-item">カテゴリー</th>
          <td class="detail-value"><%= @curriculum.category.name %></td>
        </tr>
        <tr>
          <th class="detail-item">講義の詳細</th>
          <td class="detail-value"><%= @curriculum.class_info %></td>
        </tr>
      </tbody>
    </table>
    <div class="option">
      <div class="favorite-btn">
        <%= image_tag "star.png" ,class:"favorite-star-icon" ,width:"20",height:"20"%>
        <span>お気に入り 0</span>
      </div>
      <div class="report-btn">
        <%= image_tag "flag.png" ,class:"report-flag-icon" ,width:"20",height:"20"%>
        <span>不適切な商品の通報</span>
      </div>
    </div>
  </div>
  <%# /講義の概要 %>

  <div class="comment-box">
    <form>
      <textarea class="comment-text"></textarea>
      <p class="comment-warn">
        相手のことを考え丁寧なコメントを心がけましょう。
        <br>
        不快な言葉遣いなどは利用制限や退会処分となることがあります。
      </p>
      <button type="submit" class="comment-btn">
        <%= image_tag "comment.png" ,class:"comment-flag-icon" ,width:"20",height:"25"%>
        <span>コメントする<span>
      </button>
    </form>
  </div>
  <div class="links">
    <a href="#" class="change-item-btn">
      ＜ 前の商品
    </a>
    <a href="#" class="change-item-btn">
      後ろの商品 ＞
    </a>
  </div>
  
  <a href="#" class="another-item"><%= @curriculum.category.name %>をもっと見る</a>
  
</div>

<%= render "shared/footer" %>
